schema_version = "1.0.0"

id = "automation_flow"
name = "Automation Flow"
version = "1.0.0"
tagline = "Node-based automation flow for modifier state and geometry node bake orchestration"
maintainer = "LingQS, Codex"
type = "add-on"

blender_version_min = "5.1.0"
license = [
  "SPDX:GPL-3.0-or-later",
]

[permissions]
files = "Read and write flow logs and settings in current blend file."
